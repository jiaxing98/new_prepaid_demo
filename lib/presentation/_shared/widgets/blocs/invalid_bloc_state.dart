import 'package:flutter/material.dart';

class InvalidBlocState extends StatelessWidget {
  const InvalidBlocState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Something went wrong"),
    );
  }
}
